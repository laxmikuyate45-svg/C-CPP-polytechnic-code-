//linked list creating node and print 
#include<stdio.h>
#include<stdlib.h>
typedef struct node 
{
int data ;
struct node* next;
}node;
node* create (int n);
void print(node *Head);
void count (node *Head);
void search (node *Head);

int main()
{
node *Head ;
int ch ,n;
Head=NULL ;
do 
{
printf("\n******SLL MENUE******");
printf("\n1.create node");
printf("\n2.print node");
printf("\n3.count");
printf("\n4.search");
printf("\nExit");

printf("\nenter your choice:");
scanf("%d",&ch);

switch (ch)
{

case 1:printf("\nenter size of linked list:");
       scanf("%d",&n);
       Head =create (n);
       break;
       
case 2:print(Head);
       break ;
 case 3:count(Head) ;
        break ;
        
 case 4:search(Head);  
       
 case 5:printf("\nthank for using our software");  
        break;
  default:printf("\nplease enter valid choice:");   
         break;
   }  
   }  while (ch!=3);
   
   }
   node *create (int n)
   {
   node *head ,*p;
   int x,i;
   
   
   printf("\nenter data:");
   scanf("%d",&x);
   head=(node*)malloc (sizeof(node));
   head->data =x;
   head->next =NULL ;
   p=head ;
   for(i=2;i<=n;i++)
   {
   printf("\nenter data:");
   scanf("%d",&x);
   p->next =(node*)malloc (sizeof(node));
   p=p->next;
   p->data =x;
   p->next=NULL ;
   }
   printf("\nSLL node creste successfully!!");
   return (head);
   }
   void print(node *p)
   {
   
   if (p==NULL)
   {
   printf("\nlinked list is empty:");
   
   }
   else {
   
   printf("\n**SLL nodes**");
   while (p!=NULL)
   {
   printf("%d->",p->data);
   p=p->next;
   
   }
   
   }
   
   }
   
   void count(node *head) 
   {
   if (head==NULL)
   {
   printf("linked list is empty");
   }
   else {
   node *p;
   int count=0;
   p=head;
   while(p!=NULL)
   {
   count++;
   p=p->next ;
   
   }
   
   printf("no of linked list:%d",count);
   }
   
   }
   void search (node *Head)
   {
   if (Head==NULL)
   {
   printf("\nlinked list is empty");
   
   }
   else {
   node *p;
   int key ,flag=0;
   p=Head;
   printf("enter key element for searching");
   scanf("%d",&key);
   
   while (p!=NULL)
   {
   if (key==p->data)
   {
   flag=1;
   break ;
   
   }
   p=p->next;
   
   }
   if (flag==1)
   {
   
   printf("element is found");
   }
   
   else{
   
   printf("element is not found");
   }
   
   }
   }
